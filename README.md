# 你好
這個專案使用 Next.js App Router，首頁就是閱覽室。

## 1. 頁面
首頁在 `app/page.tsx`，畫面在 `components/ReadingRoom.tsx`。

## 2. run & build
```
npm start
npm run build
```

## 3. deploy
`npm run build` 之後，把 `out/` 裡面的檔案上傳到網站根目錄。
